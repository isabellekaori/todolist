server:
  port: 9192

spring:
  datasource:
    username: root
    password: admin
    url: jdbc:mysql://localhost:3306/todolist_db
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
      format_sql: true